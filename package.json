{
  "name": "mda-popstat",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "vitest run",
    "test:watch": "vitest",
    "migrate": "node --env-file=.env.local scripts/migrate-to-blob.mjs",
    "recompress": "node --env-file=.env.local scripts/recompress-blobs.mjs"
  },
  "dependencies": {
    "@vercel/blob": "^2.3.3",
    "next": "^15.3.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
